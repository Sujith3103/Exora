name: Keep Backend Warm

on:
  schedule:
    - cron: "*/5 * * * *" # every 5 minutes
  workflow_dispatch: # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://exora-giu2.onrender.com/api/courses?category=web-development&page=1&limit=10 || true
